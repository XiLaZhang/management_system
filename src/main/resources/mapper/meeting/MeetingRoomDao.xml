<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="io.renren.modules.meeting.dao.MeetingRoomDao">

    <select id="queryRoomByMeeting" resultType="long">
        select room_id from meeting_room where meeting_id = #{meetingId}
    </select>
</mapper>